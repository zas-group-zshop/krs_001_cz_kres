name: Deploy on push
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    defaults:
      run:
        shell: powershell
    steps:
      - name: Pull to IIS folder
        run: |
          $path = 'c:\_www\bal_030_sk_seva_resp\App_Firma'
          git config --global --add safe.directory $path
          git -C $path fetch --prune
          git -C $path reset --hard origin/main